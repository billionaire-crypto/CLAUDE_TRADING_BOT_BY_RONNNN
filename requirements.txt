# ============================================================
# Trading Bot V29 - Python Dependencies
# Install with: pip install -r requirements.txt
# ============================================================
# This list matches what the code actually imports.

# Data analysis (strategy engine + backtest)
pandas>=2.0.0
numpy>=1.24.0

# Timezone handling (America/Chicago session logic)
pytz>=2023.3
# Ensures timezone database is available on minimal systems (used by pandas)
tzdata>=2023.3

# Charts / analytics output
plotly>=5.18.0

# TopstepX live connection (real-time user hub)
websocket-client>=1.7.0

# Decompressing the raw market-data download (.csv.zst)
zstandard>=0.22.0
